# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/playwright-report/
/test-results/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local
.env.test

# prisma local db
/prisma/dev.db
/prisma/dev.db-journal

# local PDF evidence storage (audit documents must not be committed to source control)
/.venshield-storage/

# typescript
*.tsbuildinfo

# enterprise modules (private — clone venshield-enterprise into /modules manually)
/modules

# copilot instructions (local dev only — do not expose to public repo)
.github/copilot-instructions.md
